# بروتوكول غرفة التحكم متعددة الذكاءات

هذا البروتوكول محايد للمنصة وقابل للنسخ إلى أي مشروع. GitHub هو المصدر التشغيلي المركزي، وكل ذكاء عامل محدود الصلاحية يعيد تقريرًا أو patch أو Pull Request معزولًا. لا يدمج أي ذكاء عمله ولا ينشره بنفسه.

## الثوابت

- فرع الإنتاج محمي، ولا يُكتب إليه مباشرة.
- لا تُرسل مفاتيح أو أسرار داخل المحادثات أو الملفات.
- يبدأ كل ذكاء بمرحلة فحص قراءة فقط.
- يصرّح الذكاء بما يستطيع فعله فعليًا وما يحتاج إعدادًا بشريًا.
- التنفيذ والدمج والنشر بوابات منفصلة، ولكل منها اعتماد صريح.
- يُربط كل ناتج برقم مهمة وcommit أساسي واسم الوكيل والاختبارات والمخاطر.
- عند إضافة ذكاء جديد، يمر بمرحلة الفحص نفسها ولا يحصل تلقائيًا على صلاحيات وكلاء سابقين.

## بطاقة ربط وكيل

يجب أن يعيد كل ذكاء هذه البطاقة قبل ربطه:

```yaml
agent_name:
platform:
access_methods: []        # API / GitHub App / CLI / webhook / scheduled task
can_read_repository: false
can_create_branch: false
can_create_pull_request: false
can_run_tests: false
can_receive_webhooks: false
can_schedule_tasks: false
requires_local_device: false
required_user_setup: []
requested_permissions: []
credential_names: []      # أسماء الأسرار فقط، لا قيمها
result_formats: []        # report / json / patch / pull_request / artifact
usage_limits:
estimated_cost_model:
security_limits: []
unsupported_capabilities: []
recommended_role:
```

## رسالة الفحص والربط الجاهزة

أرسل النص التالي لأي ذكاء، واستبدل القيم بين الأقواس فقط:

> أنت مرشح للانضمام إلى غرفة تحكم متعددة الذكاءات لمشروع `[اسم المشروع]`.
>
> المصدر الرسمي: `[رابط مستودع GitHub]`
>
> الفرع الرسمي المحمي: `[اسم الفرع]`
>
> رابط الإنتاج الرسمي: `[الرابط أو: لا يوجد]`
>
> المطلوب الآن **فحص وتصميم ربط فقط**. لا تعدّل ملفات، لا تنشئ فرعًا أو PR، لا تشغّل نشرًا، لا تطلب أو تعرض قيم أسرار، ولا تنفذ أي إجراء خارجي.
>
> 1. اقرأ ملفات التعليمات والذاكرة والتنسيق الموجودة في جذر المشروع كاملة.
> 2. افحص بنية المشروع وحالة Git ومسارات CI/CD بصلاحية قراءة فقط.
> 3. اشرح قدرات منصتك الحالية التي يمكن استخدامها فعليًا: API، GitHub App، CLI، webhooks، الجدولة، استلام النتائج، وإنشاء patches أو PRs.
> 4. ميّز بوضوح بين: متاح تلقائيًا، يحتاج إعدادًا أوليًا مني، غير مدعوم، أو غير متأكد ويحتاج توثيقًا رسميًا.
> 5. اقترح أقل صلاحيات لازمة. يحظر الوصول المباشر إلى فرع الإنتاج وأسرار النشر.
> 6. أعد «بطاقة ربط وكيل» وفق القالب الموجود في `AI_COORDINATION_PROTOCOL.md`، ثم اقترح دورك وحدودك وتكلفتك ومخاطرك.
> 7. اختم بخطة ربط قصيرة واختبار قبول آمن لا يغيّر المنتج.
>
> لا تبدأ الربط أو التنفيذ. انتظر موافقتي الصريحة بعد عرض البطاقة والخطة.

## رسالة السماح بالربط التجريبي

بعد مراجعة البطاقة، يمكن إرسال:

> أعتمد ربطك تجريبيًا بصلاحيات `[الصلاحيات المعتمدة]` فقط. نفّذ اختبار قبول غير إنتاجي للمهمة `[رقم المهمة]` على branch/worktree معزول، وأعد تقريرًا منظمًا. لا تدمج، لا تنشر، ولا توسع الصلاحيات. توقف واطلب موافقتي عند أي تعارض أو تكلفة أو صلاحية إضافية.

## رسالة تنفيذ مهمة

> المهمة: `[المعرف والعنوان]`
>
> المصدر والفرع الأساسي: `[المستودع]` / `[commit]`
>
> النطاق المسموح: `[الملفات أو الخدمات]`
>
> الممنوع: `[المسارات أو الإجراءات]`
>
> معايير القبول: `[قائمة قابلة للتحقق]`
>
> الميزانية والمهلة: `[الحدود]`
>
> افحص التعارضات والحجوزات أولًا. نفّذ فقط داخل فرع أو worktree معزول. أعد ملخصًا، الملفات المتأثرة، patch أو PR، الاختبارات الفعلية، المخاطر، والقرارات المطلوبة. لا تدمج ولا تنشر؛ انتظر اعتماد المستخدم.

## اختبار قبول أي ذكاء جديد

1. قراءة المستودع وإعادة commit الحالي دون تعديل.
2. تلخيص تعليمات المشروع وعدم مخالفتها.
3. إنتاج تقرير تجريبي منظم لمهمة قراءة فقط.
4. إثبات عدم الوصول إلى الأسرار أو فرع الإنتاج.
5. عند السماح لاحقًا: إنشاء patch صغير في فرع معزول وتشغيل الفحوص.
6. مراجعة النتيجة بشريًا ثم تثبيت دوره وحدوده.

## سجل المهمة الموحد

كل نتيجة يجب أن تتضمن: `task_id`, `agent`, `base_commit`, `summary`, `assumptions`, `files_touched`, `tests_run`, `risks`, `needs_user_decision`, `usage`, و`result_link_or_patch_hash`.
