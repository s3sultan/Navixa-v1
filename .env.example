# Public OAuth client identifier injected during local build only.
# Do not commit a real value to .env files.
GOOGLE_CLIENT_ID=

# Server-only API-Football key. Set it in the Cloudflare Worker environment; never expose it to the browser.
API_FOOTBALL_KEY=
