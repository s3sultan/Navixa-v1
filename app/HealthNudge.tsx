"use client";

/**
 * Compatibility shell for the retired standalone health nudge.
 * Health reminders now flow through PersonalReminderEngine so the user gets
 * one coordinated reminder stream instead of duplicate movement prompts.
 */
export default function HealthNudge(){
  return null;
}
