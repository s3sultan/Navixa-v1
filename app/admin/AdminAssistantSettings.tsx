"use client";import {useEffect,useState} from "react";export default function AdminAssistantSettings(){const [enabled,setEnabled]=useState(true),[saved,setSaved]=useState(false);useEffect(()=>setEnabled(localStorage.getItem("navixa-assistant-enabled")!=="false"),[]);const save=()=>{localStorage.setItem("navixa-assistant-enabled",String(enabled));document.documentElement.classList.toggle("assistant-off",!enabled);setSaved(true);setTimeout(()=>setSaved(false),1500)};return <section className="panel"><div className="panel-head"><div><small>المساعد الذكي</small><h2>تشغيل أو إيقاف المساعد</h2></div><label><input type="checkbox" checked={enabled} onChange={e=>setEnabled(e.target.checked)}/> {enabled?"مفعّل":"متوقف"}</label></div><div className="admin-actions compact"><button onClick={save}>{saved?"تم الحفظ":"حفظ الحالة"}</button></div></section>}
