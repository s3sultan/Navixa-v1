"use client";
import { useEffect, useState } from "react";
import "./usage-analytics.css";
type Cell = { grid_x: number; grid_y: number; uses: number };
type Data = { heatmap: Cell[]; settings: { retentionDays: number } };
export default function AdminUsageHeatmap() { const [data, setData] = useState<Data | null>(null); const [error, setError] = useState(""); const load = async () => { const response = await fetch("/api/admin/usage-analytics", { cache: "no-store" }); if (!response.ok) { setError("تعذر تحميل الخريطة"); return; } setData(await response.json()); }; useEffect(() => { void load(); }, []); const max = Math.max(1, ...(data?.heatmap || []).map(cell => cell.uses)); const countAt = (x: number, y: number) => data?.heatmap.find(cell => cell.grid_x === x && cell.grid_y === y)?.uses || 0; return <section className="usage-heatmap-admin" aria-label="الخريطة الحرارية المجمعة"><div className="panel-head"><div><small>مراجعة واجهة محمية</small><h2>الخريطة الحرارية المجمعة</h2><p>تفاعل تقريبي خلال آخر {data?.settings.retentionDays || 30} يومًا؛ ليست تسجيلًا للجلسة أو للشاشة.</p></div><button onClick={() => void load()}>تحديث</button></div>{error && <p className="admin-inline-notice">{error}</p>}<div className="usage-heatmap" aria-label="مناطق التفاعل المجمعة">{Array.from({ length: 12 }, (_, y) => Array.from({ length: 8 }, (_, x) => { const uses = countAt(x, y); return <i key={`${x}-${y}`} style={{ opacity: uses ? 0.16 + uses / max * 0.84 : 0.05 }} title={uses ? `${uses} تفاعلات مجمعة` : "لا تفاعلات"} />; }))}</div><div className="usage-heatmap-guide"><p><b>الأغمق:</b> منطقة شهدت تفاعلات أكثر.</p><p><b>الأفتح:</b> تفاعل أقل أو لا يوجد.</p><p><b>دقة الحماية:</b> 8 × 12 مناطق فقط؛ لا اسم عنصر، ولا نص، ولا إيميل، ولا تسجيل شاشة.</p></div></section>; }
