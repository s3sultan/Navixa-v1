"use client";import {useEffect,useState} from "react";export default function AdminCounterSettings(){const [enabled,setEnabled]=useState(true),[start,setStart]=useState(12840),[saved,setSaved]=useState(false);useEffect(()=>{const s=JSON.parse(localStorage.getItem("navixa-counter-settings")||"{}");if(s.enabled!==undefined)setEnabled(s.enabled);if(s.start)setStart(s.start)},[]);const save=()=>{localStorage.setItem("navixa-counter-settings",JSON.stringify({enabled,start}));localStorage.setItem("navixa-visit-count",String(start));setSaved(true);setTimeout(()=>setSaved(false),1500)};return <section className="panel"><div className="panel-head"><div><small>عداد الواجهة</small><h2>مرات الدخول والتجربة</h2></div><label><input type="checkbox" checked={enabled} onChange={e=>setEnabled(e.target.checked)}/> ظاهر</label></div><div className="admin-actions compact"><input type="number" value={start} onChange={e=>setStart(Number(e.target.value))}/><button onClick={save}>{saved?"تم الحفظ":"حفظ وتحديث العداد"}</button></div></section>}
