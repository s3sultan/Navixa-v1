"use client";import {useEffect,useState} from "react";export default function AdminHealthSettings(){const [enabled,setEnabled]=useState(true),[minutes,setMinutes]=useState(45),[saved,setSaved]=useState(false);useEffect(()=>{const p=JSON.parse(localStorage.getItem("navixa-health-policy")||"{}");if(p.enabled!==undefined)setEnabled(p.enabled);if(p.minutes)setMinutes(p.minutes)},[]);const save=()=>{localStorage.setItem("navixa-health-policy",JSON.stringify({enabled,minutes}));setSaved(true);setTimeout(()=>setSaved(false),1500)};return <section className="panel"><div className="panel-head"><div><small>تنبيهات الصحة</small><h2>توقيت تذكير الحركة</h2></div><label><input type="checkbox" checked={enabled} onChange={e=>setEnabled(e.target.checked)}/> مفعّل</label></div><div className="admin-actions compact"><label>بعد <select value={minutes} onChange={e=>setMinutes(Number(e.target.value))}><option value="30">30 دقيقة</option><option value="45">45 دقيقة</option><option value="60">60 دقيقة</option><option value="90">90 دقيقة</option></select></label><button onClick={save}>{saved?"تم الحفظ":"حفظ الإعداد"}</button></div></section>}
