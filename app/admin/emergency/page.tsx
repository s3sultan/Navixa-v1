"use client";
import {useState} from "react";
import "../../emergency/emergency.css";
import { useAdminAuth } from "../useAdminAuth";
export default function AdminEmergency(){const { allowed, checking } = useAdminAuth();const [services,setServices]=useState([{name:"المساعد الذكي",on:true},{name:"الاستماع الذكي",on:true},{name:"متابعة الشاشة",on:true},{name:"الأتمتة",on:true},{name:"الإعلانات",on:true}]);if (checking || !allowed) return null;return <main className="emergency" dir="rtl"><header><a href="/admin">← لوحة الإدارة</a><span>مركز الطوارئ الخاص</span><b className="em-brand"><img src="/navixa-mark.png" alt="" /> NAVIXA</b></header><section className="em-hero"><span>⚠</span><div><small>حالة المنصة</small><h1>التحكم العاجل بالخدمات</h1><p>هذه الصفحة لا تظهر للعامة، وهي متاحة لإيميل الإدارة فقط.</p></div><b>● مستقرة</b></section><section className="em-grid">{services.map((s,i)=><article key={s.name}><span>{s.on?"✓":"!"}</span><div><b>{s.name}</b><small>{s.on?"تعمل بشكل طبيعي":"متوقفة إداريًا"}</small></div><button className={s.on?"stop":"start"} onClick={()=>setServices(services.map((x,j)=>j===i?{...x,on:!x.on}:x))}>{s.on?"إيقاف":"تفعيل"}</button></article>)}</section></main>}
