import { NextResponse } from "next/server.js";
import {clean as cleanReferral,createAttribution} from "../../../referrals.ts";
import { resolveAdminJwtSecret } from "../../../../worker/adminAuth.ts";
import { clientIp, consumeAuthRateLimit } from "../../../../worker/authRateLimit.ts";
import { getUserAuthSettings, resolveUserSession } from "../../../../worker/userAuth.ts";
import { buildMoyasarCheckout, type PaymentMethodId } from "../../../billing/providers/index.ts";

type D1Statement={bind:(...values:unknown[])=>D1Statement;all:<T=Record<string,unknown>>()=>Promise<{results:T[]}>;run:()=>Promise<unknown>};
type D1Database={prepare:(sql:string)=>D1Statement};type Env=Record<string,string|undefined>;
type Settings={provider:string;mode:string;public_checkout:string;test_webhook_enabled:string;live_payments_enabled:string;card_mada_enabled:string;visa_enabled:string;mastercard_enabled:string;apple_pay_enabled:string;stc_pay_enabled:string;samsung_pay_enabled:string;tamara_enabled:string;tamara_sandbox_enabled:string};
type Plan="monthly"|"sprint";type FoundersAward={intent_id:string;contact:string;user_id:string;price_amount:number;status:string;expires_at:string};
const defaults:Settings={provider:"moyasar",mode:"test",public_checkout:"false",test_webhook_enabled:"false",live_payments_enabled:"false",card_mada_enabled:"true",visa_enabled:"true",mastercard_enabled:"true",apple_pay_enabled:"false",stc_pay_enabled:"false",samsung_pay_enabled:"false",tamara_enabled:"false",tamara_sandbox_enabled:"true"};
const planDetails={monthly:{amount:2500,label:"NAVIXA Plus · شهر واحد",days:30},sprint:{amount:1200,label:"NAVIXA Sprint · خمسة أيام",days:5}} as const;
const windows=new Map<string,{count:number;resetAt:number}>();const clean=(value:unknown,limit:number)=>typeof value==="string"?value.replace(/\s+/g," ").trim().slice(0,limit):"";const validEmail=(value:string)=>/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value);const changed=(value:unknown)=>((value as {meta?:{changes?:number}})?.meta?.changes||0);
function allowedRate(request:Request){const id=clientIp(request),now=Date.now(),current=windows.get(id);if(!current||current.resetAt<now){windows.set(id,{count:1,resetAt:now+10*60_000});return true}if(current.count>=5)return false;current.count+=1;return true}
function rateLimitResponse(retryAfter=600){return NextResponse.json({error:"عدد محاولات كبير، حاول لاحقًا"},{status:429,headers:{"Retry-After":String(retryAfter),"Cache-Control":"no-store"}})}
async function db():Promise<D1Database|null>{try{return (await import("cloudflare:workers") as {env?:{DB?:D1Database}}).env?.DB||null}catch{return (globalThis as {DB?:D1Database}).DB||null}}async function env():Promise<Env>{try{return (await import("cloudflare:workers") as {env?:Env}).env||{}}catch{return globalThis as Env}}
async function schema(database:D1Database){await database.prepare("CREATE TABLE IF NOT EXISTS navixa_billing_settings (setting_key TEXT PRIMARY KEY,setting_value TEXT NOT NULL,updated_at TEXT NOT NULL)").run();await database.prepare("CREATE TABLE IF NOT EXISTS navixa_billing_intents (id TEXT PRIMARY KEY,user_id TEXT NOT NULL DEFAULT '',contact TEXT NOT NULL,plan TEXT NOT NULL,amount INTEGER NOT NULL,currency TEXT NOT NULL DEFAULT 'SAR',mode TEXT NOT NULL DEFAULT 'test',status TEXT NOT NULL DEFAULT 'pending',provider_payment_id TEXT NOT NULL DEFAULT '',referral_code TEXT NOT NULL DEFAULT '',discount_code TEXT NOT NULL DEFAULT '',founders_intent_id TEXT NOT NULL DEFAULT '',created_at TEXT NOT NULL,updated_at TEXT NOT NULL,expires_at TEXT NOT NULL)").run();await database.prepare("ALTER TABLE navixa_billing_intents ADD COLUMN discount_code TEXT NOT NULL DEFAULT ''").run().catch(()=>{});await database.prepare("ALTER TABLE navixa_billing_intents ADD COLUMN founders_intent_id TEXT NOT NULL DEFAULT ''").run().catch(()=>{});await database.prepare("CREATE TABLE IF NOT EXISTS navixa_discount_codes (id TEXT PRIMARY KEY,code TEXT NOT NULL UNIQUE,discount_type TEXT NOT NULL DEFAULT 'percent',discount_value INTEGER NOT NULL DEFAULT 0,plans TEXT NOT NULL DEFAULT 'all',max_redemptions INTEGER NOT NULL DEFAULT 0,redeemed_count INTEGER NOT NULL DEFAULT 0,reserved_count INTEGER NOT NULL DEFAULT 0,valid_from TEXT NOT NULL DEFAULT '',valid_until TEXT NOT NULL DEFAULT '',enabled INTEGER NOT NULL DEFAULT 1,created_at TEXT NOT NULL,updated_at TEXT NOT NULL)").run();await database.prepare("ALTER TABLE navixa_discount_codes ADD COLUMN reserved_count INTEGER NOT NULL DEFAULT 0").run().catch(()=>{});const now=new Date().toISOString();for(const [key,value] of Object.entries(defaults))await database.prepare("INSERT OR IGNORE INTO navixa_billing_settings (setting_key,setting_value,updated_at) VALUES (?,?,?)").bind(key,value,now).run()}
async function settings(database:D1Database){await schema(database);const rows=await database.prepare("SELECT setting_key,setting_value FROM navixa_billing_settings").all<{setting_key:string;setting_value:string}>();const next={...defaults};for(const row of rows.results)if(row.setting_key in next)next[row.setting_key as keyof Settings]=row.setting_value;return next}
export async function GET(){const database=await db();if(!database)return NextResponse.json({available:false,message:"الاشتراك المدفوع يفتح قريبًا."},{headers:{"Cache-Control":"no-store"}});const current=await settings(database),secrets=await env();const available=current.mode==="live"&&current.public_checkout==="true"&&current.live_payments_enabled==="true"&&Boolean(secrets.MOYASAR_LIVE_PUBLISHABLE_KEY);return NextResponse.json({available,message:available?"الدفع الآمن متاح الآن":"الاشتراك المدفوع يفتح قريبًا."},{headers:{"Cache-Control":"no-store"}})}
export async function POST(request:Request){if(request.headers.get("origin")!==new URL(request.url).origin)return NextResponse.json({error:"طلب غير مسموح"},{status:403});if(!allowedRate(request))return rateLimitResponse();const database=await db();if(!database)return NextResponse.json({error:"الدفع غير متاح حاليًا"},{status:503});const pepper=await resolveAdminJwtSecret();if(pepper){const shared=await consumeAuthRateLimit(database,"billing-checkout-ip",clientIp(request),pepper,5,10*60_000);if(!shared.allowed)return rateLimitResponse(shared.retryAfterSeconds)}const current=await settings(database);if(current.public_checkout!=="true"||current.live_payments_enabled!=="true"||current.mode!=="live")return NextResponse.json({error:"الاشتراك المدفوع يفتح قريبًا."},{status:409});const authSettings=await getUserAuthSettings(database).catch(()=>null),session=await resolveUserSession(request,database);if(!authSettings?.userAuthEnabled||!session)return NextResponse.json({error:"سجّل الدخول بحساب NAVIXA قبل المتابعة إلى الدفع"},{status:401});const secrets=await env(),publicKey=secrets.MOYASAR_LIVE_PUBLISHABLE_KEY||"";if(!publicKey)return NextResponse.json({error:"إعداد الدفع غير مكتمل"},{status:503});const body=await request.json().catch(()=>({})) as {plan?:unknown;referralCode?:unknown;discountCode?:unknown;foundersIntentId?:unknown};const contact=session.email,plan=clean(body.plan,20) as Plan,referralCode=cleanReferral(body.referralCode,20).toUpperCase(),discountCode=clean(body.discountCode,32).toUpperCase(),foundersIntentId=clean(body.foundersIntentId,80);if(!validEmail(contact)||!(plan in planDetails))return NextResponse.json({error:"بيانات الاشتراك غير صالحة"},{status:400});if(foundersIntentId&&plan!=="monthly")return NextResponse.json({error:"سعر المؤسس يطبق على Plus الشهري فقط"},{status:400});if(foundersIntentId&&discountCode)return NextResponse.json({error:"لا يمكن دمج سعر المؤسس مع كود خصم آخر"},{status:400});const now=new Date(),details=planDetails[plan],intentId=crypto.randomUUID();let amount=details.amount,discountApplied="";if(foundersIntentId){const awards=await database.prepare("SELECT intent_id,contact,user_id,price_amount,status,expires_at FROM navixa_founders_awards WHERE intent_id=? AND contact=? AND user_id=? AND status='reserved' AND expires_at>? LIMIT 1").bind(foundersIntentId,contact,session.userId,now.toISOString()).all<FoundersAward>();const award=awards.results[0];if(!award)return NextResponse.json({error:"سعر المؤسس غير متاح أو انتهت مهلة الحجز"},{status:409});amount=award.price_amount}else if(discountCode){await database.prepare("UPDATE navixa_discount_codes SET reserved_count=(SELECT COUNT(*) FROM navixa_billing_intents WHERE discount_code=? AND status='pending' AND expires_at>?),updated_at=? WHERE code=?").bind(discountCode,now.toISOString(),now.toISOString(),discountCode).run();const rows=await database.prepare("SELECT code,discount_type,discount_value,plans,max_redemptions,redeemed_count,reserved_count,valid_from,valid_until,enabled FROM navixa_discount_codes WHERE code=? LIMIT 1").bind(discountCode).all<{code:string;discount_type:string;discount_value:number;plans:string;max_redemptions:number;redeemed_count:number;reserved_count:number;valid_from:string;valid_until:string;enabled:number}>();const coupon=rows.results[0],nowMs=Date.now(),from=coupon?.valid_from?new Date(coupon.valid_from).getTime():0,until=coupon?.valid_until?new Date(coupon.valid_until+"T23:59:59Z").getTime():0,alreadyUsed=await database.prepare("SELECT id FROM navixa_billing_intents WHERE contact=? AND discount_code=? AND status='paid' LIMIT 1").bind(contact,discountCode).all<{id:string}>(),eligible=Boolean(coupon&&coupon.enabled&&(!from||nowMs>=from)&&(!until||nowMs<=until)&&(!coupon.max_redemptions||coupon.redeemed_count+coupon.reserved_count<coupon.max_redemptions)&&(coupon.plans==="all"||coupon.plans===plan)&&!alreadyUsed.results.length);if(!eligible)return NextResponse.json({error:"كود الخصم غير صالح أو انتهى أو تم استخدامه لهذا الحساب"},{status:400});const reduction=coupon.discount_type==="fixed"?coupon.discount_value:Math.round(details.amount*coupon.discount_value/100);amount=Math.max(100,details.amount-reduction);discountApplied=coupon.code;const reserved=await database.prepare("UPDATE navixa_discount_codes SET reserved_count=reserved_count+1,updated_at=? WHERE code=? AND enabled=1 AND (max_redemptions=0 OR redeemed_count+reserved_count<max_redemptions)").bind(now.toISOString(),discountCode).run();if(changed(reserved)===0)return NextResponse.json({error:"اكتمل عدد استخدامات كود الخصم"},{status:409})}const expires=new Date(now.getTime()+15*60_000).toISOString();await database.prepare("INSERT INTO navixa_billing_intents (id,user_id,contact,plan,amount,currency,mode,status,provider_payment_id,referral_code,discount_code,founders_intent_id,created_at,updated_at,expires_at) VALUES (?,?,?,?,?,'SAR','live','pending','',?,?,?,?,?,?)").bind(intentId,session.userId,contact,plan,amount,referralCode,discountApplied,foundersIntentId,now.toISOString(),now.toISOString(),expires).run();const attribution=referralCode?await createAttribution(database,referralCode,contact,intentId).catch(()=>null):null;const enabledMethods:PaymentMethodId[]=[];if(current.card_mada_enabled!=="false")enabledMethods.push("mada");if(current.visa_enabled!=="false")enabledMethods.push("visa");if(current.mastercard_enabled!=="false")enabledMethods.push("mastercard");if(current.apple_pay_enabled==="true")enabledMethods.push("applepay");if(current.stc_pay_enabled==="true")enabledMethods.push("stcpay");if(current.samsung_pay_enabled==="true")enabledMethods.push("samsungpay");const hasCardNetwork=enabledMethods.some(method=>method==="mada"||method==="visa"||method==="mastercard");const supportedNetworks=[...(current.card_mada_enabled!=="false"?["mada"]:[]),...(current.visa_enabled!=="false"?["visa"]:[]),...(current.mastercard_enabled!=="false"?["mastercard"]:[]),...(hasCardNetwork?["amex","unionpay"]:[])];const origin=new URL(request.url).origin;const callbackPath=plan==="sprint"?"/plus/complete":"/plus/complete";const checkout=buildMoyasarCheckout({id:intentId,amount,currency:"SAR",description:foundersIntentId?"NAVIXA Plus · سعر مؤسس":details.label,callbackUrl:`${origin}${callbackPath}?intent=${encodeURIComponent(intentId)}`,metadata:{navixa_intent:intentId,navixa_plan:plan,navixa_duration_days:String(details.days),navixa_founders_intent:foundersIntentId}},publicKey,enabledMethods);if(checkout.state!=="ready")return NextResponse.json({error:"لا توجد وسيلة دفع مفعلة حاليًا"},{status:503});return NextResponse.json({intentId,provider:checkout.provider,publicKey:checkout.publicKey,amount:checkout.amount,currency:checkout.currency,description:foundersIntentId?"NAVIXA Plus · سعر مؤسس":details.label,durationDays:details.days,callbackUrl:checkout.callbackUrl,metadata:checkout.metadata,methods:checkout.methods,supportedNetworks,applePay:checkout.applePay,referralApplied:Boolean(attribution)},{headers:{"Cache-Control":"no-store"}})}
