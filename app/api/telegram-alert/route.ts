import {NextResponse} from "next/server";
export async function POST(request:Request){try{const {name}=await request.json();if(!name||typeof name!=="string")return NextResponse.json({error:"اسم غير صالح"},{status:400});const token=process.env.TELEGRAM_BOT_TOKEN,chatId=process.env.TELEGRAM_CHAT_ID;if(!token||!chatId)return NextResponse.json({error:"Telegram غير مربوط"},{status:503});const response=await fetch(`https://api.telegram.org/bot${token}/sendMessage`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({chat_id:chatId,text:`تنبيه: سمعنا اسمك (${name.slice(0,80)})`})});if(!response.ok)return NextResponse.json({error:"تعذر إرسال التنبيه"},{status:502});return NextResponse.json({ok:true})}catch{return NextResponse.json({error:"تعذر معالجة التنبيه"},{status:500})}}
